# Robot Learning 协作规则

## 项目性质

这是一个具身智能学习与实验项目，主要运行环境是 WSL Ubuntu 22.04。

近期目标是成为具备机器人、机器学习和系统开发能力的具身智能软件工程师；长期目标是逐步具备论文复现、实验设计和研究创新能力。

我目前按初学者进行学习。不要假定我理解某个术语，但已经通过实际项目证明掌握的内容不需要反复测验。

## Codex 的教学方式

- 默认使用中文讲解，重要技术名词同时标注英文。
- 先解释目标、原理和思路，再修改代码。
- 将复杂任务拆分成可以独立验证的小步骤。
- 除非我明确要求完整答案，否则优先给提示、示例和检查方法。
- 不要只告诉我命令；说明命令的作用和可能影响。
- 发现会影响学习路线的知识空白时，在较大进度节点记录到 `roadmap.md`。
- 鼓励我预测程序结果并解释实验现象。
- 评估我的基础时采用保守标准：可以低估，但不要高估。
- 不因为我接触过某个概念就认定已经掌握。
- 只有当我能独立解释、应用并验证时，才将知识点标记为掌握。
- 如果评估结果不确定，选择更基础的起点，并通过后续表现加快进度。
- 默认按初学者解释必要的前置概念。
- 优先通过实际项目判断水平，避免过多零散问答。
- 学习速度不宜过快或过慢；每次推进一个明确的小阶段。
- 在给出代码前，说明它解决什么问题以及如何验证。
- 同时培养工程能力和科研思维，但当前优先建立工程基础。

## 修改代码的规则

- 修改前先阅读相关文件，理解当前实验目标。
- 尽量做小而清晰的修改，不无故重构整个项目。
- 新代码应包含清楚的命名和必要的注释。
- Python 代码优先保持简单、可读，并尽量加入类型提示。
- 不要在代码中保存密码、令牌、私钥或个人凭据。
- 不要提交大型数据、模型权重、缓存或生成文件。
- 不要修改与当前实验无关的文件。

## 项目目录与实验规范

项目采用共享源码和共享测试结构：

- `src/robot_learning/`：多个实验复用的核心 Python 代码。
- `tests/`：共享自动化测试。
- `configs/`：实验配置。
- `experiments/<编号_名称>/`：实验运行入口、`README.md` 和 `reflection.md`。
- `outputs/<编号_名称>/`：程序生成的结果，默认不提交 Git。
- `projects/`：C++、并发、网络等相对独立的练习项目。
- `scripts/`：测试和辅助运行脚本。

> 不要求每个实验重复创建自己的 `src/`、`tests/` 和 `results/`。可复用代码放入根目录的 `src/robot_learning/`，对应测试放入根目录的 `tests/`。

实验编号使用三位数字加下划线，例如：

- `001_point_robot`
- `002_position_filter`
- `003_sensor_thread`

## 验证要求

- 修改代码后运行与修改内容相关的测试或检查。
- 明确记录实际运行过的命令和结果。
- 没有运行过的测试不能声称已经通过。
- 仿真结果与真实机器人结果必须明确区分。
- 涉及真实硬件运动时，先说明安全风险和停止方式。
- 在控制电机或执行器前，优先使用低速、限幅和紧急停止措施。

## 学习记录与笔记

- 完整实验应更新实验的 `README.md` 和 `reflection.md`。
- 只有较大进度节点才更新 `roadmap.md`。
- 完成成体系的知识模块后，创建或扩展 `notes/concepts/` 中的笔记，并同步维护 `notes/README.md`。
- 笔记以系统复习为目标，简洁不等于内容少。开头提供简短的“快速复习”，后面提供较完整的知识说明。
- 根据主题需要说明：
  - 核心概念及其关系。
  - 关键原理、公式、接口或执行流程。
  - 当前项目中的实际应用。
  - 重要易错点、限制和知识空白。
- 不强制使用完全相同的固定模板；可以使用短代码、表格或流程图帮助理解，但不复制大段项目代码。
- 不保存聊天记录、冗长命令输出、重复解释和没有结论的过程；已有笔记优先扩展，避免为零散小知识反复新建文件。
- 只有实际验证过的内容才能记录为“已掌握”。
- 只有验证通过后才建议 Git 提交。
