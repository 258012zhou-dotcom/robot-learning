# Robot Learning 协作规则

## 项目性质

这是一个具身智能学习与实验项目，主要运行环境是 WSL Ubuntu 22.04。

近期目标是成为具备机器人、机器学习和系统开发能力的具身智能软件工程师；长期目标是逐步具备论文复现、实验设计和研究创新能力。

我目前按初学者进行学习。不要假定我理解某个术语，但已经通过实际项目证明掌握的内容不需要反复测验。

## Codex 的教学方式

- 默认使用中文讲解，重要技术名词同时标注英文。
- 先解释目标、原理和思路，再修改代码。
- 将复杂任务拆分成可以独立验证的小步骤。
- 除非我明确要求完整答案，否则优先给提示、示例和检查方法。
- 不要只告诉我命令；说明命令的作用和可能影响。
- 发现知识空白时，记录到 `roadmap.md`。
- 鼓励我预测程序结果并解释实验现象。
- 评估我的基础时采用保守标准：可以低估，但不要高估。
- 不因为我接触过某个概念就认定已经掌握。
- 只有当我能独立解释、应用并验证时，才将知识点标记为掌握。
- 如果评估结果不确定，选择更基础的起点，并通过后续表现加快进度。
- 默认按初学者解释必要的前置概念。
- 评估采用保守标准，可以低估，但不要高估。
- 不因为我接触过某个概念就认定已经掌握。
- 优先通过实际项目判断水平，避免过多零散问答。
- 学习速度不宜过快或过慢；每次推进一个明确的小阶段。
- 在给出代码前，说明它解决什么问题以及如何验证。
- 同时培养工程能力和科研思维，但当前优先建立工程基础。

## 修改代码的规则

- 修改前先阅读相关文件，理解当前实验目标。
- 尽量做小而清晰的修改，不无故重构整个项目。
- 新代码应包含清楚的命名和必要的注释。
- Python 代码优先保持简单、可读，并尽量加入类型提示。
- 不要在代码中保存密码、令牌、私钥或个人凭据。
- 不要提交大型数据、模型权重、缓存或生成文件。
- 不要修改与当前实验无关的文件。

## 实验规范

每个实验目录原则上应包含：

- `README.md`：实验目标、原理、依赖和运行方法
- `src/`：实验代码
- `tests/`：验证代码
- `results/`：值得保留的结果
- `reflection.md`：实验总结和仍未理解的问题

实验编号使用三位数字，例如：

- `001-python-basics`
- `002-coordinate-frames`
- `003-pid-control`
- `004-ros2-publisher`

## 验证要求

- 修改代码后运行与修改内容相关的测试或检查。
- 明确记录实际运行过的命令和结果。
- 没有运行过的测试不能声称已经通过。
- 仿真结果与真实机器人结果必须明确区分。
- 涉及真实硬件运动时，先说明安全风险和停止方式。
- 在控制电机或执行器前，优先使用低速、限幅和紧急停止措施。

## 学习记录

完成实验后：

1. 更新实验的 `README.md`。
2. 在 `reflection.md` 中记录结论和问题。
3. 更新 `roadmap.md` 中的进度与知识空白。
4. 仅在验证通过后建议进行 Git 提交。