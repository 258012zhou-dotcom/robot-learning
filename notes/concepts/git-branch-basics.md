# Git 分支与合并

`main` 是稳定主线；分支是在当前提交上开出的独立修改路线。

- `git switch -c 名称`：创建并切换分支。
- `git switch main`：回到主线。
- `git merge 分支名`：把指定分支合入当前分支。

个人学习中的小实验可直接在 `main` 完成；需要尝试高风险方案或较大改动时，再创建分支隔离尝试。

合并时若两条分支改了同一位置，Git 会要求人工解决冲突。先理解两边意图、运行检查，再完成合并；不要用覆盖方式直接丢掉未知改动。
