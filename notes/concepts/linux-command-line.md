# Linux 命令行基础

## 已掌握

- `~`：当前用户的主目录
- `.`：当前目录
- `..`：上一级目录
- `pwd`：显示当前目录
- `cd`：切换目录
- `ls -la`：查看文件、文件夹和隐藏内容
- `find . -maxdepth 2 -type d | sort`：查看一定层级内的文件夹
- `grep -n "关键词" 文件名`：在文件中搜索关键词，并显示行号
