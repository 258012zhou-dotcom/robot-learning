# Python 可靠软件边界

- 类型提示用于表达预期的数据类型，但不会自动完成运行时检查。
- JSON 等外部输入应先经过验证，再进入核心计算。
- `PointRobotConfig` 将松散字典转换为字段明确的配置对象。
- 配置缺失、形状错误或数值非法时，应尽早抛出带清楚信息的 `ValueError`。
- 核心数值函数仍保留自己的输入验证，避免错误数据继续传播。
- `logging` 用于记录正常流程、关键参数、结果和错误。
- 捕获异常时应能够恢复或补充上下文，不应使用空的 `except` 隐藏问题。